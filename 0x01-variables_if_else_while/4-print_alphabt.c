#include <stdio.h>

/**
* main - prit alphabet
*
* Return: Always 0
*/


int main(void)
{

	char hn;

	for (hn = 'a' ; hn <= 'z' ; hn++)
	{
		if(hn != e && hn !=q)
		putchar(hn);
	}
	putchar('\n');
	return (0);
}
