#include <stdio.h>

/**
* main - print all possible numprs
*
* Return: Always 0
*/


int main(void)
{

	int a;

	for (a = '0' ; a <= '9' ; num++)
	{
		putchar(num);
        if (a != 9)
        putchar(',');
        putchar('');
	}
	putchar('\n');
	return (0);
}
